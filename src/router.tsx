import React from 'react'

// Minimal router placeholder. If you use `react-router-dom`, replace this
// implementation with a proper BrowserRouter + Routes setup.

export const AppRouter: React.FC<{ children?: React.ReactNode }> = ({ children }) => {
  return <>{children}</>
}

export default AppRouter
